<div class="bg-gray-50 rounded-lg shadow-sm p-4 mb-4 hover:shadow-md transition-shadow duration-200">
  <div class="flex items-center justify-between mb-2">
    <div class="flex items-center">
      <div class="drag-handle mr-2">☰</div>
      <h3 class="text-lg font-medium text-gray-800"><%= section.name %></h3>
    </div>
    <div class="flex items-center space-x-3">
      <%= link_to edit_section_path(section), class: "text-blue-500 hover:text-blue-700" do %>
        <i class="fas fa-edit text-lg"></i>
      <% end %>
      <%= link_to section_path(section), data: { turbo_method: :delete, confirm: 'Are you sure you want to delete this section?' }, class: "text-red-500 hover:text-red-700" do %>
        <i class="fas fa-trash text-lg"></i>
      <% end %>
    </div>
  </div>
</div>
